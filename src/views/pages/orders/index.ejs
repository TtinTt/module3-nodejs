<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Rikkei Academy</title>
  </head>

  <body>
    <div>
      <h1><%= title %></h1>
      <a href="/orders/new">Thêm mới</a>
      <table>
        <thead>
          <th>ID</th>
          <th>Tên đăng nhập</th>
          <th>Email</th>
          <th>Hành động</th>
        </thead>
        <tbody>
          <% for (let order of orders) { %>
          <tr>
            <td><%= order.id %></td>
            <td><%= order.ordername %></td>
            <td><%= order.email %></td>
            <td>
              <a href="/orders/<%= order.id %>/edit">Sửa</a>
              <form
                action="/orders/<%= order.id %>?_method=DELETE"
                method="POST"
              >
                <button type="submit">Xóa</button>
              </form>
            </td>
          </tr>
          <% } %>
        </tbody>
      </table>
    </div>
  </body>
</html>
